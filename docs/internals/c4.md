# C4-style overview — ux-channel

> **Diátaxis:** explanation · **Canonical:** `docs/internals/c4.md` · **Layer:** ux-channel
> Map: [../INDEX.md](../INDEX.md).

Grounded in [../../ARCHITECTURE.md](../../ARCHITECTURE.md). This layer owns
Intent / Result / Cap / wire / peers / Cap Host (cek-runtime; ≠ HTTP Product host). It does **not** own HTML trees or CSS.

Cap machine = **cek-runtime Host** + Channel façade (`CekHostCapService`).
There is no live `HostRuntime` / `PeerApply`.

## Context

```mermaid
flowchart TB
  Human[Human / agent / MCP]
  subgraph this["ux-channel (this repo)"]
    Intent["Intent {action, args, cap}"]
    Host["Channel host / registry"]
    Cap["cek-runtime Host (façade)"]
    Result["Result {ok, ops[]}"]
    Wire["JSON floor + CXB"]
  end
  Dom["ux-dom — HTML trees"]
  Behavior["ux-behavior — product behavior"]
  Motion["ux-motion — presence plans"]
  Compose["ux-compose — product CLI"]
  Human --> Intent --> Host --> Result
  Host --> Cap
  Host --> Wire
  Dom -.->|"does not own Intent"| this
  Behavior -->|"verified Ops; not codecs"| Result
  Motion -->|"peels transition.* ; Channel never learns them"| Result
  Compose -.->|"composition, not wire"| this
```

## Containers (this repo)

```mermaid
flowchart LR
  SPEC["SPEC/ + conformance/"] --> Py["python/ host"]
  SPEC --> Rs["rust/ classic floor"]
  Py --> ASGI["asgi / HTTP"]
  Py --> Caps["CekHostCapService"]
  Caps --> Cek["cek-runtime Host"]
  Rs --> Peer["classic Peer (verify-only)"]
```

Existing layout diagram: [../../ARCHITECTURE.md](../../ARCHITECTURE.md).
