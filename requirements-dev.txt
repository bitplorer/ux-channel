# Minimal deps for monorepo gate (./verify.sh / CI)
itsdangerous>=2.1
pytest>=8.0
msgpack>=1.0
hypothesis>=6.100
pytest-cov>=5.0
# Default Cap machine (cut #3). Escape: UX_CHANNEL_CEK=off
# PyPI cek-host 0.1.3 still exports cek_host.legal. Missing stamp there
# ignores strict mode. The catalog (cek_host.catalog) is cek-python main,
# not released. Pin that commit so the gate matches the code.
cek-host @ git+https://github.com/bitplorer/cek-python.git@0626a10b36535942f9687dbe2eb298742263a780#subdirectory=cek-host
cek-surface @ git+https://github.com/bitplorer/cek-python.git@0626a10b36535942f9687dbe2eb298742263a780#subdirectory=cek-surface
# Host suite (optional — make test-python-host)
fastapi>=0.100
httpx>=0.24
starlette>=0.27
pytest-asyncio>=0.23
