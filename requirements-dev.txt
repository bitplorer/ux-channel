# Minimal deps for monorepo gate (./verify.sh / CI)
itsdangerous>=2.1
pytest>=8.0
msgpack>=1.0
hypothesis>=6.100
# Host suite (optional — make test-python-host)
fastapi>=0.100
httpx>=0.24
starlette>=0.27
pytest-asyncio>=0.23
