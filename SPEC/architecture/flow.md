# Flow (stub)

**Canonical:** [trace.md](trace.md) — CEK VOCAB **trace** is correlation, never authority.

`meta.flow_id` is the wire-immortal alias of `meta.trace`. Do not treat flow as a
peer engine or a Cap. Config `flow` / env `FLOW` alias `trace`.

See [ADR 0007](ADR/0007-flow-correlation-only.md).
