# ADR 0002 — Classic ops are the permanent floor

## Decision
IR classic leaves remain valid forever. Rich forms are projected opt-ins.

## Consequences
Old peers never require upgrades for basic UI. `effects: "classic"` freezes encoding.
