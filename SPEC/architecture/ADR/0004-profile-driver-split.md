# ADR 0004 — Profile + driver terminology

## Decision
Use **profile** (vocabulary claim) and **driver** (implementation). Avoid “capability” for profiles (clashes with Cap).

## Consequences
Hello carries profile ids; runtime loads driver packs.
