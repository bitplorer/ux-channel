# ADR 0007 — Flow is correlation only

## Decision
`meta.flow_id` is default-opt-in tagging for multi-step host work. Peers ignore unknown meta. No peer Flow engine.

## Consequences
Backward compatible; resume is host-side.
