[pytest]
# Monorepo gate config (do not inherit host-package asyncio extras).
testpaths = python/tests
pythonpath = python
addopts = -q --tb=line
filterwarnings =
    ignore::pytest.PytestConfigWarning
