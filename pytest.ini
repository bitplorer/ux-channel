[pytest]
# Monorepo gate only — full suite under python/tests/* is opt-in
testpaths = python/tests/gate
pythonpath = python/src
addopts = -q --tb=line
norecursedirs = examples scripts packages .git
filterwarnings =
    ignore::pytest.PytestConfigWarning
