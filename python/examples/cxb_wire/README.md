# CXB wire example

Opt-in binary format for channel Intent / Result (same dicts as JSON).

```bash
# from repo root
PYTHONPATH=src python examples/cxb_wire/demo.py
```

Browsers keep JSON. Use CXB for Python agents, workers, or batch when size matters.
