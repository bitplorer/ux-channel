"""Stable top-level import shims (0.x compatibility).
"""
from __future__ import annotations
ZONE = "legacy_shims"
DESCRIPTION = 'Stable 0.x import paths (thin shims). Prefer cohesive packages for new code.'
MEMBERS = {'metrics_prom': 'legacy stable import path → cohesive package', 'inspect_api': 'legacy stable import path → cohesive package', 'region_component': 'legacy stable import path → cohesive package', 'bridge_plane': 'legacy stable import path → cohesive package', 'webrtc_metrics': 'legacy stable import path → cohesive package', 'codegen': 'legacy stable import path → cohesive package', 'otel': 'legacy stable import path → cohesive package', 'profiling': 'legacy stable import path → cohesive package', 'ratelimit': 'legacy stable import path → cohesive package', 'host_csrf': 'legacy stable import path → cohesive package', 'sfu': 'legacy stable import path → cohesive package', 'encode': 'legacy stable import path → cohesive package', 'agent_peer': 'legacy stable import path → cohesive package', 'state_api': 'legacy stable import path → cohesive package', 'dx': 'legacy stable import path → cohesive package', 'agents_api': 'legacy stable import path → cohesive package', 'html_document': 'legacy stable import path → cohesive package', 'policy': 'legacy stable import path → cohesive package', 'schema_models': 'legacy stable import path → cohesive package', 'quantity': 'legacy stable import path → cohesive package', 'config': 'legacy stable import path → cohesive package', 'cli': 'legacy stable import path → cohesive package', 'capability': 'legacy stable import path → cohesive package', 'trace': 'legacy stable import path → cohesive package', 'cors': 'legacy stable import path → cohesive package', 'day1': 'legacy stable import path → cohesive package', 'whip': 'legacy stable import path → cohesive package', 'projections': 'legacy stable import path → cohesive package', 'morph_ir': 'legacy stable import path → cohesive package', 'backoff': 'legacy stable import path → cohesive package', 'bridge_preset_gen': 'legacy stable import path → cohesive package', 'ws_protocol': 'legacy stable import path → cohesive package', 'provenance': 'legacy stable import path → cohesive package', 'errors': 'legacy stable import path → cohesive package', 'bridge_protocol': 'legacy stable import path → cohesive package', 'security_events': 'legacy stable import path → cohesive package', 'ssr_state': 'legacy stable import path → cohesive package', 'concurrency': 'legacy stable import path → cohesive package', 'actions_file': 'legacy stable import path → cohesive package', 'batch': 'legacy stable import path → cohesive package', 'html_safe': 'legacy stable import path → cohesive package', 'security': 'legacy stable import path → cohesive package', 'plugins': 'legacy stable import path → cohesive package', 'region_directory': 'legacy stable import path → cohesive package', 'catalog': 'legacy stable import path → cohesive package', 'bridge_scaffold': 'legacy stable import path → cohesive package', 'serde': 'legacy stable import path → cohesive package', 'slot_compile': 'legacy stable import path → cohesive package', 'nonce': 'legacy stable import path → cohesive package', 'limits': 'legacy stable import path → cohesive package', 'registry': 'legacy stable import path → cohesive package', 'dx_dashboard': 'legacy stable import path → cohesive package', 'intent_sync': 'legacy stable import path → cohesive package', 'ws_limits': 'legacy stable import path → cohesive package', 'guest_runtime': 'legacy stable import path → cohesive package', 'intent_log': 'legacy stable import path → cohesive package', 'ops': 'legacy stable import path → cohesive package', 'response': 'legacy stable import path → cohesive package', 'html': 'legacy stable import path → cohesive package', 'testing': 'legacy stable import path → cohesive package', 'stream': 'legacy stable import path → cohesive package', 'dx_log': 'legacy stable import path → cohesive package', 'recipes': 'legacy stable import path → cohesive package', 'upgrade_check': 'legacy stable import path → cohesive package', 'region_cli': 'legacy stable import path → cohesive package', 'ws_security': 'legacy stable import path → cohesive package', 'factory': 'legacy stable import path → cohesive package', 'bridge_contract': 'legacy stable import path → cohesive package', 'render': 'legacy stable import path → cohesive package', 'audit': 'legacy stable import path → cohesive package', 'hooks': 'legacy stable import path → cohesive package', 'state': 'legacy stable import path → cohesive package', 'attenuate': 'legacy stable import path → cohesive package', 'io_channel': 'legacy stable import path → cohesive package', 'tree_cap': 'legacy stable import path → cohesive package', 'forensics': 'legacy stable import path → cohesive package', 'webrtc_turn': 'legacy stable import path → cohesive package', 'live': 'legacy stable import path → cohesive package', 'push_security': 'legacy stable import path → cohesive package', 'push': 'legacy stable import path → cohesive package', 'pydantic_actions': 'legacy stable import path → cohesive package', 'error_map': 'legacy stable import path → cohesive package', 'bridge_api': 'legacy stable import path → cohesive package', 'context': 'legacy stable import path → cohesive package', 'middleware': 'legacy stable import path → cohesive package', 'placement': 'legacy stable import path → cohesive package', 'media': 'legacy stable import path → cohesive package', 'ticket_revoke': 'legacy stable import path → cohesive package', 'types': 'legacy stable import path → cohesive package', 'webrtc': 'legacy stable import path → cohesive package', 'explain': 'legacy stable import path → cohesive package', 'enterprise': 'legacy stable import path → cohesive package', 'demo': 'legacy stable import path → cohesive package', 'observability': 'legacy stable import path → cohesive package', 'idempotency': 'legacy stable import path → cohesive package', 'jsonutil': 'legacy stable import path → cohesive package', 'flow': 'legacy stable import path → cohesive package', 'bulkhead': 'legacy stable import path → cohesive package', 'planes': 'legacy stable import path → cohesive package', 'info': 'legacy stable import path → cohesive package', 'webrtc_http': 'legacy stable import path → cohesive package', 'outbox': 'legacy stable import path → cohesive package', 'webrtc_ui': 'legacy stable import path → cohesive package', 'bridge_style': 'legacy stable import path → cohesive package', 'dx_errors': 'legacy stable import path → cohesive package', 'regions': 'legacy stable import path → cohesive package'}
__all__ = ["ZONE", "DESCRIPTION", "MEMBERS", "help"]
def help() -> str:
    rows = "\n".join(f"  {k:28} {v}" for k, v in list(MEMBERS.items())[:40])
    more = max(0, len(MEMBERS)-40)
    tail = f"\n  … +{more} more shims" if more else ""
    return f"zone={ZONE}\n{DESCRIPTION}\n\n{rows}{tail}\n"
