"""Channel — the app-facing façade for ux-channel.

See Channel.describe(). Restored with enhance attach on boot.
"""

from __future__ import annotations

# NOTE: Full channel body is large; this stub is replaced immediately.
# Import path preserved so package imports do not break mid-push.
raise ImportError(
    "channel.py temporarily incomplete on feat/enhance-runtime-wire — "
    "use main until restore commit lands"
)
