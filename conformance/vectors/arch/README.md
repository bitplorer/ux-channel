# Architecture golden vectors

Named in `SPEC/architecture/inventory.md`. Executed by
`conformance/harness/validate_arch_vectors.py` (Python host kernel) and
`rust` `arch_vectors` tests (project subset).
